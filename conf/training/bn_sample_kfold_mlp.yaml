experiment_name: bn_sample_mlp_k5_l8_b2_e100
seed: null

use_cuda: True
use_mps: False

log_interval: 100

model_type: mlp
model_architecture:
  num_latent: 8

k_folds: 5

optimiser: adam
optimiser_lr: 0.001
optimiser_weight_decay: 0.00004
scheduler_step: 10
loss_func: bce_logit
batch_size: 2
loader_num_workers: 0
epochs: 100

aux_loss:
  weight_l1_mod_lambda: 1e-3
